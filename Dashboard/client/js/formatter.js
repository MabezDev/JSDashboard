// save

//from DOM element we need to get this: 
/*
	widget = {
		title : "Temperature Widget 2k16",
		serviceURL : "yahoo.weather.api/search......",
		variables = [
						{ key: "Temp", jsonKey: "weather.temperature" },
						{ key: "Humid", jsonKey : "weather.humidity" }
					]
	}
*/


function domWidgetToJSON(domElement) {
  //var serviceURL = domElement.
}

// load
